{
  "id": "intuition-1.3b",
  "name": "Intuition Extension",
  "version": "1.3b",
  "author": "Andrew Church",
  "source": "binary",
  "tokenSource": "itokens.s",
  "assembleDefines": [],
  "idBase": 6,
  "idBaseEvidence": "calibrated",
  "evidence": "source",
  "docs": [
    "cmdlist",
    "Intuition.guide",
    "todo",
    "routine.codes",
    "screenmodes.txt"
  ],
  "titleStrings": [
    "The AMOS Intuition Extension - Version 1.3b"
  ],
  "observedSlots": [
    14
  ],
  "provenance": {
    "from": "AMOS-Professional-Official/extensions/Intuition-41.95/Intuition",
    "note": "Distributed as 'Intuition-41.95' — 41.95 is the intuition.library version it targets, not the extension version. Intuition.guide and the makefile both say 1.3b, and the guide says who wrote it: \"The Intuition Extension is copyright © 1994-1995 Andrew Church.\" This manifest said Peter Hickman until 2026-08-21; PAUL Hickman is one of eleven beta testers the guide thanks, and the name was taken from that list. THE AUTHOR'S OWN SOURCE IS VENDORED, 26 files under src/, and it is complete: every one of the 267 L_ labels itokens.s names is defined in them. So are both shipped binaries, from the Ultimate Amiga 'ie13b' archive — APSystem/Intuition.lib is the AMOS Pro build and AMOS_System/Intuition.lib the AMOS 1.3 one — and the guide, the author's `todo`, his `bugs` list and `routine.codes`. The table here is assembled from itokens.s and agrees with the AMOS Pro binary on every id, name and spec: 0 differences across 183 named entries, re-checked 2026-08-21. The AMOS 1.3 binary differs on exactly the five names itokens.s's own `ifd CREATOR` conditional predicts, which is the three-way check that source, binaries and table are one release. The table is read from APSystem/Intuition.lib rather than assembled from itokens.s as of 2026-08-21: a source-assembled table has no routine numbers -- `tokensFromSource` puts a placeholder where the linker would put an address -- so nothing could cite a routine or check that it had. The two tables are identical, so this changes no name, id or spec.",
    "redistribution": "Ships with the AMOS Professional source release."
  },
  "findings": "TWO BUILDS SHIP, and they disagree on five keyword NAMES. The ie13b archive carries Intuition.lib under both AMOS_System/ (the AMOS 1.3 build) and APSystem/ (the AMOS Pro build). Every token id, parameter spec and handler number is identical between them and to this table; five names are not. At $003e and $004e the AMOS 1.3 build spells them as one word where AMOS Pro spells them as two, and at $05a0/$05ac/$05bc the AMOS 1.3 build puts the 'i' after 'font' where AMOS Pro puts it before. This table follows the AMOS Pro build, which is the right default for AMOS Pro programs; a program written under AMOS 1.3 that used those five would not tokenise against it. The same AMOS-versus-AMOSPro split shows up in IntuiExtend, which ships 269-keyword and 294-keyword builds of one release. The id base is not a guess: exactly one offset in -2048..2048 (K=6) maps all 149 distinct slot-14 token ids observed across the 488-program corpus onto valid entry starts. See src/ext/registry.test.ts. Andrew Burton's AMOS Extensions List (kept at ultimateamiga.com, updated 2007-07-29), compiled from the AMOS-LIST archives 1994-1998, Amiga magazine clippings and coverdisk CDs. lists slot 25 but asks 'Slot 14 originally?' — the corpus answers it: every program here that uses Intuition has it at slot 14. THE SLOT IS 14 AND THE BINARY SAYS 15, and the source says which is right. `defs.i`:39 is `ExtNum equ 14`, and `macros.i` reaches the extension's own zone with `ExtAdr+(ExtNum-1)*16(a5)` -- so ExtNum is ONE-based, exactly as Int 1.0's `$278` is `$f8 + 16*24` for slot 25. But `errors.s`:68 and :134 pass `moveq #ExtNum,d2` into L_ErrorExt, and AMOS wants d2 ZERO-based there. DEFECT, and Andrew Church's own: every error this extension raises is attributed to extension 15 rather than to itself, so a library in slot 15 has its message table indexed by Intuition's error number. The manifest recorded 25 from Andrew Burton's list, which itself asks 'Slot 14 originally?'; the guide answers it -- \"enter \\\"Intuition.Lib\\\" in extension slot number 14\" -- and so do all 488 corpus programs and defs.i.",
  "notes": "Opens real Intuition screens and windows, including HAM, EHB, SuperHires and AGA modes.",
  "recommendedSlot": 14,
  "library": "Intuition.lib",
  "format": "legacy",
  "tokenSourceNote": "src/itokens.s assembles to this same table -- 0 differences across 183 named entries -- and the table is taken from the BINARY now so that routine NUMBERS are real and a citation can be checked against the code. `idBase` 6 was the offset that made the assembled table line up; the binary needs none."
}
