node_modules/
dist/
dist-web/
fixtures/
# and as a symlink, which "fixtures/" does not match: staging the corpus
# by symlinking it is the usual way to run the fixtures-gated tests
fixtures
*.log

# throwaway one-off scripts while reading a binary — never committed
src/cli/tmp/

# noise made by claude cli
.claude

# the headless-Firefox probe page, which has to sit where Vite can resolve
# the source imports and is rewritten for every question it answers
probe.html
# temporary reports for coverage
report.md

# temporary state for the keyword audit; only its instructions belong in Git
audit/*
!audit/README.md
!audit/LUNA.md
